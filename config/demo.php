<?php

return [

    /*
    |---------------------------------------------------------------------------
    | Akun Demo
    |---------------------------------------------------------------------------
    |
    | NPM of the account that /demo signs visitors in as, so they can try the
    | app before subscribing. Leave empty to turn the demo off entirely — the
    | route 404s and the landing page hides its demo call to action.
    |
    */

    'npm' => env('DEMO_NPM'),

];
